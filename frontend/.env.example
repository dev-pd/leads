# Browser API base. Behind nginx this is same-origin "/api".
# For bare `npm run dev` without nginx, use http://localhost:8000/api
NEXT_PUBLIC_API_BASE_URL=/api

# Server-side (server components / route handlers) reach the backend directly.
BACKEND_INTERNAL_URL=http://localhost:8000
